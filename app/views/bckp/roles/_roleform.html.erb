<%
  # form_for is a rails helper for rendering forms in the view  
  # @role is an instance scoped variable that is declared in 
  # roles_controller new method. It points to a new
  # instance of the Film model
  # f.submit will need a create action
%>


<%= form_for @role do |f| %>
  <%= f.label :name %><br>
  <%= f.text_field :name %><br>
  <br>
  <%= f.submit %>
<% end %>